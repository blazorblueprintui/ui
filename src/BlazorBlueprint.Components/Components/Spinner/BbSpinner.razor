@namespace BlazorBlueprint.Components

@*
    Spinner component - displays a loading indicator.
    Uses CSS animation for the spinning effect.
*@

<svg class="@CssClass"
     xmlns="http://www.w3.org/2000/svg"
     fill="none"
     viewBox="0 0 24 24"
     role="status"
     aria-label="@(AriaLabel ?? "Loading")"
     @attributes="AdditionalAttributes">
    <circle class="bb:opacity-25"
            cx="12"
            cy="12"
            r="10"
            stroke="currentColor"
            stroke-width="4">
    </circle>
    <path class="bb:opacity-75"
          fill="currentColor"
          d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z">
    </path>
</svg>

@code {
    /// <summary>
    /// The size of the spinner.
    /// </summary>
    [Parameter]
    public SpinnerSize Size { get; set; } = SpinnerSize.Default;

    /// <summary>
    /// Additional CSS classes to apply to the spinner.
    /// </summary>
    [Parameter]
    public string? Class { get; set; }

    /// <summary>
    /// Accessible label for the spinner.
    /// Default is "Loading".
    /// </summary>
    [Parameter]
    public string? AriaLabel { get; set; }

    /// <summary>
    /// Additional attributes to apply to the spinner.
    /// </summary>
    [Parameter(CaptureUnmatchedValues = true)]
    public Dictionary<string, object>? AdditionalAttributes { get; set; }

    /// <summary>
    /// Gets the computed CSS classes for the spinner.
    /// </summary>
    private string CssClass => ClassNames.cn(
        "bb:animate-spin",
        Size switch
        {
            SpinnerSize.Small => "bb:h-4 bb:w-4",
            SpinnerSize.Default => "bb:h-6 bb:w-6",
            SpinnerSize.Large => "bb:h-10 bb:w-10",
            _ => "bb:h-6 bb:w-6"
        },
        HasCustomColor ? null : "bb:text-muted-foreground",
        Class
    );

    private bool HasCustomColor => !string.IsNullOrEmpty(Class) && Class.Contains("text-");
}
