<BbBarChart Data="@data" Height="300px"
            OnDataPointClick="HandlePointClick"
            OnChartClick="ClearSelection">
    <BbXAxis DataKey="date" />
    <BbYAxis />
    <BbBar DataKey="desktop" Name="Desktop" Color="var(--chart-2)" />
</BbBarChart>

@code {
    private string? selection;

    private void HandlePointClick(ChartClickEventArgs e)
    {
        // DataIndex maps straight back to the collection you bound,
        // which is what makes a drill-down straightforward.
        var row = data[e.DataIndex];
        selection = $"{e.SeriesName} — {e.Name}: {e.Value}";
    }

    private void ClearSelection() => selection = null;
}
