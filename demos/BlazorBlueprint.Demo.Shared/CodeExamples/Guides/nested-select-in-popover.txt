<BbPopover>
    <BbPopoverTrigger>
        <BbButton Variant="ButtonVariant.Outline">Open popover</BbButton>
    </BbPopoverTrigger>
    <BbPopoverContent Class="w-[320px]">
        <BbSelect TValue="string"
                  @bind-Value="selectedFruit"
                  Options="@fruitOptions"
                  Placeholder="Select a fruit"
                  Class="w-full" />
    </BbPopoverContent>
</BbPopover>

@* No configuration. The select renders through the same portal host as the popover,
   and the popover's dismiss listener treats an interaction that started in a nested
   portal as inside itself. *@
