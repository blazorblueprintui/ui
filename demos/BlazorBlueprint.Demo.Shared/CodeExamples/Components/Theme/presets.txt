// Startup defaults:
builder.Services.AddBlazorBlueprintComponents(configureTheme: options =>
{
    options.DefaultPreset = ThemePresets.Compact with { PrimaryColor = PrimaryColor.Blue };
});

// Runtime changes:
await themeService.SetPresetAsync(ThemePresets.Glass with { DarkMode = true });
await themeService.SetDesignAsync(themeService.Preset.Design with { MenuAccent = ThemeMenuAccent.Primary });

// Show all appearance selectors:
<BbThemeSwitcher ShowDesignOptions="true" />
