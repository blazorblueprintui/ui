@* AM/PM uses BbSelect; the clock popup uses the same styled selectors. *@
<BbTimeInput @bind-Value="value" Format="TimeFormat.Hour12" AriaLabel="Appointment time" />

@code {
    private TimeSpan? value = new TimeSpan(14, 30, 0);
}
