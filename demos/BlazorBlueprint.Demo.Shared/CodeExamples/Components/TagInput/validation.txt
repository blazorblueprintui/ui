<BbTagInput @bind-Tags="tags"
            MaxTags="5"
            MaxTagLength="20"
            Placeholder="Max 5 tags, 20 chars each..."
            OnTagRejected="HandleRejected" />

@code {
    private IReadOnlyList<string>? tags;

    private void HandleRejected(string tag)
    {
        Console.WriteLine($"Rejected: {tag}");
    }
}