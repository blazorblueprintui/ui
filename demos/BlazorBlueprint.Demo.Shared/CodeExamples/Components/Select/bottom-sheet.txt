<BbSelect TValue="string" @bind-Value="sheetChoice" Presentation="SelectPresentation.BottomSheet"
          SheetTitle="Delivery method" Placeholder="Choose delivery" Options="deliveryOptions" Class="w-64" />

@code {
    private string? sheetChoice;
    private readonly SelectOption<string>[] deliveryOptions = [new("standard", "Standard delivery"), new("express", "Express delivery"), new("pickup", "Collect in store")];
}
