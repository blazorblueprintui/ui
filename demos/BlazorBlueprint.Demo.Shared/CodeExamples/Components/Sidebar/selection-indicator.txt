<BbSidebarProvider CookieKey="@((string?)null)" HeightClass="min-h-0" EnableToggleShortcut="false">
    <div class="relative w-64 rounded-lg border bg-sidebar p-2">
        <BbSidebarSelectionIndicator FollowHover="true" />
        <BbSidebarMenu>
            @foreach (var itemLabel in new[] { "Overview", "Projects", "Settings" })
            {
                <BbSidebarMenuItem>
                    <BbSidebarMenuButton IsActive="@(indicatorPage == itemLabel)" OnClick="@(() => indicatorPage = itemLabel)" Class="relative bg-transparent! hover:bg-transparent! data-[active=true]:bg-transparent!">
                        <span>@itemLabel</span>
                    </BbSidebarMenuButton>
                </BbSidebarMenuItem>
            }
        </BbSidebarMenu>
    </div>
</BbSidebarProvider>

@code { private string indicatorPage = "Overview"; }
