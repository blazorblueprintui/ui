@* Paste is on by default. Focus the upload, then press Ctrl+V (Cmd+V on macOS). *@
<BbFileUpload Multiple="true" @bind-Files="files" />

@* Turn it off if the page handles paste itself and the two would compete. *@
<BbFileUpload Multiple="true" AllowPaste="false" @bind-Files="files" />
